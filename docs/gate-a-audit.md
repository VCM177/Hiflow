# Kiểm định Cổng A

Ngày: 2026-09-24 Nhánh: develop Commit: 0af6d54 Cây làm việc: có thay đổi chưa commit (`.claude/hooks/guard.mjs`, `.claude/skills/gate-audit/`, `docs/gate-a-audit.md`, `docs/business-flow.md`)

## Kết luận

**CỔNG ĐÃ ĐÓNG**: 19 PASS, 0 FAIL, 0 SKIPPED, 0 DEVIATION.

e2e toàn bộ 2026-09-24 với Supabase và Redis thật (`npm run test:e2e`, chạy nền): **31/31 suite, 556/556 test pass, exit 0**, 898 s, không có test nào bị bỏ qua. Ghi chú: một lượt chạy trước đó bị dừng nhưng tiến trình vẫn chạy song song, ghi chung tệp log, và có 8 test fail (409 khi xếp lịch, do hai lượt cùng giành một khung giờ của cùng một phỏng vấn viên). Lượt đó không được tính là bằng chứng. Không chạy hai lượt e2e cùng lúc.

Lần chạy toàn bộ gần nhất (2026-09-21, trước khi có khóa): 541 pass, 10 fail, 5 skip. Cả 10 lỗi nằm ở `dashboard.e2e-spec.ts` và đều do mất kết nối tới Neon (`getaddrinfo ENOTFOUND`). Chạy lại riêng `dashboard` thì 26/26 pass. Lần chạy toàn bộ trước đó nữa (cùng ngày) cho 549 pass, 0 fail, 5 skip, exit 0.

| ID   | Hạng mục                                                                | Kết quả | Bằng chứng                                                                                                                                                                                                                                                                                                                                                                                              |
| ---- | ----------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1  | CV lên bucket Supabase riêng tư, Signed URL 60 giây                     | PASS    | 2026-09-24: `npm run test:e2e -w @hiflow/api -- supabase-storage throttle-redis`, exit 0, 5/5 pass. Supabase thật: tải lên, tải qua Signed URL (đúng nội dung, có tên tệp), địa chỉ công khai của cùng đối tượng bị từ chối, xóa rồi tải trả 404. Khóa là `service_role`. Lần chạy đầu thất bại vì khóa cũ là `anon` (RLS chặn ghi); đã đổi khóa. `SIGNED_URL_SECONDS = 60` (`supabase.storage.ts:19`). |
| 1.2  | Tệp đổi đuôi `.pdf` nhưng nội dung không phải PDF bị chặn               | PASS    | `cv-file.validator.spec.ts` (exe "MZ" đổi thành `.pdf` và `.docx`, script, HTML, zip thường, `.doc`), guard spec (ASF không tới `file-type`), e2e `candidates` và `public-apply` trả 400, không lưu gì.                                                                                                                                                                                                 |
| 1.3  | Bộ đếm rate limit ở Redis (Upstash), đếm chính xác                      | PASS    | 2026-09-24: cùng lệnh trên. Upstash thật: hai kết nối dùng chung một bộ đếm (lần thứ 3 bị chặn với giới hạn 2), và ứng dụng chọn kho Redis khi có `REDIS_URL` ("Rate limiting through Redis"). Khóa thử có tiền tố `e2e-` và đã được dọn.                                                                                                                                                               |
| 1.4  | CORS không dùng `origin: '*'`                                           | PASS    | Grep `origin: '*'` và `origin: true` trong `apps/api/src`: 0 kết quả. `cors.ts` từ chối `*`; `cors.spec.ts`, `security.e2e-spec.ts`.                                                                                                                                                                                                                                                                    |
| 1.5  | Cookie phiên có `Secure`, `HttpOnly`, `SameSite=Strict`                 | PASS    | `session-cookie.spec.ts`; `auth-cookie.e2e-spec.ts` kiểm header `Set-Cookie` thật khi đăng nhập và đăng xuất.                                                                                                                                                                                                                                                                                           |
| 2.1  | Mọi unit test và e2e pass, gồm vòng đời trọn vẹn                        | PASS    | `npm test` exit 0 (309/309). `npm run test:e2e` exit 0: 556/556, gồm `lifecycle.e2e-spec.ts` và các suite Supabase, Redis thật.                                                                                                                                                                                                                                                                         |
| 2.2  | Trưởng bộ phận chỉ đọc dữ liệu phòng mình; gọi chéo bị từ chối          | PASS    | Đã quyết định: ngoài phạm vi trả **404** (`business-flow.md` mục 7, điểm 8). Bằng chứng: `requisitions.e2e-spec.ts:250,264`, mục scoping của `applications.e2e-spec.ts`, `common/scope.spec.ts`. Trưởng bộ phận không có quyền xem ứng viên (403), chỉ xem hồ sơ thuộc tin của phòng mình; đây là thiết kế đã có.                                                                                       |
| 2.3  | Phỏng vấn viên chỉ ghi kết quả phỏng vấn của mình; mọi CRUD khác bị 403 | PASS    | Đã quyết định: họ được xem ứng viên, hồ sơ, lịch và CV trong phạm vi lịch của mình (`business-flow.md` mục 7, điểm 9). Bằng chứng: `rbac-matrix.e2e-spec.ts` (mọi route × 5 vai trò), `interviews.e2e-spec.ts:394` (lịch người khác trả 404), `:404` (vai trò khác trả 403).                                                                                                                            |
| 2.4  | Không thể thêm route mà không quyết định quyền                          | PASS    | `rbac-matrix.e2e-spec.ts` ("finds the whole API", "leaves no route open"). Đã thử gỡ tạm một `@RequirePermissions`: test bắt được đúng route.                                                                                                                                                                                                                                                           |
| 3.1  | `seed:base` và `seed:demo` exit 0, chạy lần hai không đổi gì            | PASS    | Trên CSDL hiện có: cả hai lệnh chạy hai lần, exit 0, lần hai "0 new rows" và "nothing changed". `seed-demo.e2e-spec.ts` (idempotent, reset an toàn).                                                                                                                                                                                                                                                    |
| 3.1b | Chạy được từ CSDL **trống**                                             | PASS    | 2026-09-24: tạo database tạm `gate_scratch_empty` (0 bảng) → `migration:run` exit 0 (7 migration) → `seed:base` hai lần exit 0 (3 phòng ban, 8 vị trí, 5 người dùng; lần hai 0 dòng mới) → `seed:demo` hai lần exit 0 (5 yêu cầu, 3 tin, 15 ứng viên, 15 hồ sơ, 6 phỏng vấn, 3 offer; lần hai không đổi) → đã xóa database tạm.                                                                         |
| 3.2  | Chỉ dữ liệu hư cấu                                                      | PASS    | `seed-demo.e2e-spec.ts` ("uses only fictional example.com addresses"). Ghi chú: số điện thoại demo sinh theo công thức, đúng định dạng nên về lý thuyết có thể trùng số thật.                                                                                                                                                                                                                           |
| 3.3  | 5 người dùng (5 vai trò), 3 phòng ban, 8 vị trí                         | PASS    | CSDL trống sau seed: 3 phòng ban, 8 vị trí, 5 người dùng + 1 tài khoản hệ thống (do migration tạo).                                                                                                                                                                                                                                                                                                     |
| 3.4  | Mọi trạng thái phễu có dữ liệu demo                                     | PASS    | `seed-demo.e2e-spec.ts:54`: NEW 4, SCREENING 4, INTERVIEW 3, OFFER 2, HIRED 1, REJECTED 1.                                                                                                                                                                                                                                                                                                              |
| 3.5  | Mốc thời gian lịch sử tăng nghiêm ngặt, không trùng                     | PASS    | `seed-demo.e2e-spec.ts` dùng `toBeGreaterThan` (đã siết từ `>=`), pass.                                                                                                                                                                                                                                                                                                                                 |
| 3.6  | `appliedAt` trải trong 30 ngày gần nhất                                 | PASS    | `seed-demo.e2e-spec.ts` ("spreads the application dates over the last 30 days"), pass.                                                                                                                                                                                                                                                                                                                  |
| 4.1  | `/docs` liệt kê mọi endpoint                                            | PASS    | `swagger.e2e-spec.ts` ("documents exactly the routes the application serves"): tập route trong OpenAPI bằng đúng 76 route thật.                                                                                                                                                                                                                                                                         |
| 4.2  | `servers`: localhost, `/api`, production                                | PASS    | `swagger.e2e-spec.ts` ("lists where the API lives", và không có production khi thiếu `PUBLIC_API_URL`).                                                                                                                                                                                                                                                                                                 |
| 4.3  | 401, 403, 429 (có `Retry-After`) ghi đúng chỗ, chỉ ở chỗ đó             | PASS    | Các khối `401`, `403`, `429` trong `swagger.e2e-spec.ts`. Tồn đọng đã biết: chưa mô tả phản hồi thành công, chỉ mô tả lỗi.                                                                                                                                                                                                                                                                              |

## Cần quyết định

Không còn. Hai điểm lệch đã được người dùng quyết định ngày 2026-09-24 và ghi ở `docs/business-flow.md` mục 7, điểm 8 và 9.

## Việc còn mở

Không còn mục nào của Cổng A. Ngoài danh sách cổng, cần biết trước khi triển khai: chưa gửi thử qua Resend thật, thư hiện chỉ đi qua driver `outbox`.
